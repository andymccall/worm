; Worm - (c) 2026 Andy McCall
; Licensed under CC BY-NC 4.0
; https://creativecommons.org/licenses/by-nc/4.0/

; ---------------------------------------------------------------------------
; wm_equates.inc - Shared constants for all modules
; ---------------------------------------------------------------------------
; Include this file in any module that needs game constants.
; Using .include for constants is the standard cc65 pattern since
; .import/.export doesn't work for assemble-time numeric equates.
; ---------------------------------------------------------------------------

.ifndef WM_EQUATES_INC
WM_EQUATES_INC = 1

; ---------------------------------------------------------------------------
; Direction constants
; ---------------------------------------------------------------------------

DIR_NONE  = 0
DIR_UP    = 1
DIR_DOWN  = 2
DIR_LEFT  = 3
DIR_RIGHT = 4

; ---------------------------------------------------------------------------
; Action input codes (returned by platform_poll_input)
; ---------------------------------------------------------------------------

INPUT_PAUSE = 5
INPUT_QUIT  = 6

; ---------------------------------------------------------------------------
; Screen dimensions (pixels)
; ---------------------------------------------------------------------------

SCREEN_W = 320
SCREEN_H = 240

; ---------------------------------------------------------------------------
; Grid setup
; ---------------------------------------------------------------------------

CELL_SIZE  = 8             ; pixels per grid cell
GRID_X     = 12            ; pixel offset of first cell column
GRID_Y     = 26            ; pixel offset of first cell row (below status bar)
GRID_COLS  = 37            ; grid width in cells
GRID_ROWS  = 25            ; grid height in cells

; ---------------------------------------------------------------------------
; Movement timing (frames between moves)
; ---------------------------------------------------------------------------

MOVE_DELAY = 8

; ---------------------------------------------------------------------------
; Color indices
; ---------------------------------------------------------------------------

COLOR_BLACK = 0

; ---------------------------------------------------------------------------
; Maximum worm length
; ---------------------------------------------------------------------------

MAX_LENGTH = 255

; ---------------------------------------------------------------------------
; Spider constants
; ---------------------------------------------------------------------------

MAX_SPIDERS = 8

; ---------------------------------------------------------------------------
; Lives cap
; ---------------------------------------------------------------------------

MAX_LIVES = 3

; ---------------------------------------------------------------------------
; Border / status bar layout
; ---------------------------------------------------------------------------

BORDER_X1  = 10
BORDER_Y1  = 10
BORDER_X2  = 309
BORDER_Y2  = 229
DIVIDER_Y  = 24

; ---------------------------------------------------------------------------
; Status bar text pixel positions
; ---------------------------------------------------------------------------

STATUS_FOOD_X  = 14
STATUS_FOOD_Y  = 20
STATUS_LIVES_X = 236
STATUS_LIVES_Y = 20

; ---------------------------------------------------------------------------
; Status bar hearts
; ---------------------------------------------------------------------------

STATUS_HEART_X_START = 278
STATUS_HEART_Y       = 14
STATUS_HEART_SPACING = 10
STATUS_HEART_MAX     = 6

.endif
